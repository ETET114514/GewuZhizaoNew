# GewuZhizaoNew

## 灰底图家具漏检修正（2026-09-14）

修正灰底被固定阈值误当作线条的问题，并扩充已有家具示例图的八个模板目标。该图现在检出左下衣柜、餐桌、厨房三段柜台、水槽、灶台和冰箱，共 23 件家具与设施候选。页面已支持冰箱编辑，56 项测试通过。结果见 [灰底图核查](wall-recognition/output/gray-furniture-v1/README.md)。这是同来源图例校准，仍存在其他漏检和误检，所有候选均需校核。

## 跨图家具识别与卫浴补检（2026-09-14）

现已结合本地图例匹配与 ONNX 模型识别家具。线稿参考图检出 20 件候选，包含床、沙发、桌椅、柜体，以及新增的马桶、洗手台和待确认洗浴区；默认彩色图保留 24 件候选。所有自动结果仍需校核，不能视为完整清单或通用准确率。

模板搜索和门边支撑判断已提速，同图重复识别可复用缓存，编辑结果与缓存隔离。实测完整线稿识别约 19 秒，默认彩色图约 12 秒。功能说明、测试及限制见 [项目说明](wall-recognition/README.md)，最新叠加图见 [线稿结果](wall-recognition/output/bathroom-v1/reference-04.png)。

首次克隆后，请按 [模型安装说明](wall-recognition/models/README.md) 生成本地 ONNX 文件；仓库不包含模型权重、虚拟环境或用户保存的项目。模型未安装时仍可运行图例匹配。模型来源和许可约束也记录在该说明中。

## 家具与设施补充（2026-09-13）

新增茶几、餐桌、鞋柜、电视柜、厨房柜台、水槽、灶台、浴室柜、马桶与洗浴区类别。两个卫生间按用户要求标为“洗浴区待确认”，可在页面改成浴缸或淋浴间；自动结果不会替用户确认。

餐桌借助周围座椅辅助匹配，但导出的框只包含桌面。水槽、灶台和所在柜台分别保留，均可编辑、撤销和保存。新类别使用默认原图的显式参考裁切，仍是同画法校准，不代表任意户型的通用识别。新版结果位于 `wall-recognition/output/fixtures-v1/`。以下为前次版本记录。

## 家具识别修正（2026-09-13）

默认示例图现在检出三张床、三处挂衣式衣柜，以及客厅的一张长沙发和两把单人座椅（共九件候选）。修正了茶几局部误报为沙发的问题；页面新增柜子类别、橙色框，支持修改、补入、排除、撤销和保存。

这次将默认图中的七个家具裁切加入显式模板库，并调整纹理密度过滤与小尺寸细节校验。默认图是校准样本，不能把九件命中当作跨户型准确率；柜子仍不覆盖任意橱柜、电视柜或收纳柜。结果与回归说明见 [家具第二版](wall-recognition/output/furniture-v2/README.md)。下方第一版记录保留为历史说明。

本地户型墙体识别与校正工作台，作为室内设计平台的第一阶段。

当前流程：**导入户型图 → 粗识别墙体 → 识别门窗 → 保留洞口并整理实墙 → 修改、校核、保存**。

2026-09-09 更新：支持白底双线墙，并恢复粗墙交接处遗漏的局部墙带。窗候选避开已识别的双线墙；无法区分窗、栏杆或设备的边界须确认后才扣墙。六张参考图的对比与已知缺口见 [参考图核对说明](wall-recognition/input/reference-plans/README.md)，结果在 `wall-recognition/output/reference-v2/`。密集尺寸图仍有短隔墙漏检。

## 家具识别第一版（2026-09-10）

新增床、沙发的本地图例匹配，支持缩放、四向旋转和镜像。页面可查看 F 编号、改类别与框坐标、调整床头/靠背方向、排除误报和框选补家具，共用撤销与保存。支持的输入仍是图片，尚未增加 DWG/DXF 解析。

测试页面：[家具示例](http://127.0.0.1:8771/?sample=furniture)（需启动 8771 端口服务）。默认双击启动端口仍为 8765；也可在页面点击“载入家具示例图”。保存项目新增 `furniture.json`。图例来源和当前限制见 [家具图例说明](wall-recognition/input/furniture-references/README.md)。

当前是候选识别原型；不同画法仍会漏检。参考图自身的匹配结果不能视为通用准确率。

## 启动

需要 Python 3.10 或以上。

```powershell
cd wall-recognition
python -m pip install -r requirements.txt
python web_server.py --open-browser
```

Windows 也可以在安装依赖后双击 [start-review.cmd](wall-recognition/start-review.cmd)。本地地址为 [http://127.0.0.1:8765/](http://127.0.0.1:8765/)，保持程序运行即可使用。

## 已实现

- 上传 PNG、JPG、WebP，或使用附带的示例户型图。
- 调用 Python 图像处理程序，提取带独立编号的水平、垂直墙体候选。
- 按平行窗框、墙端及开启弧线提取门窗候选，支持显示切换、定位、确认门/窗、类别待定及排除误报。
- 门窗反过来约束墙体：补齐洞口连接，保留有像素支撑的短墙；延长、补墙后重新扣除洞口，页面和实墙导出保持一致。
- 点选墙段后，按方向、像素数或目标墙直接延长、缩短、补转角、移动、改厚度、删除。
- 框选漏识别区域后直接补墙，支持撤销最近 50 次修改。
- 保存修改后的墙段坐标、底图和操作记录到本机 `wall-recognition/projects/`。

例如：选择 W005，指定“向右、连接到 W004”，点击应用，页面程序直接更新墙段。

## 当前边界

这是早期原型。门窗采用几何规则识别，家具结合图例和模型识别，仍有漏检、误检和类别歧义。尺寸单位为像素，尚未校准真实比例。文字输入只解析部分明确的方向、目标编号和像素数。家具与设施支持人工校正；井道分类、三维建模和渲染尚未实现。已保存项目暂未支持从页面重新导入。

最新三步流程结果在 `wall-recognition/output/pipeline-v1/`：26 段粗墙、10 处门窗候选，整理后为 29 段实墙（包含补出的 3 段短墙）。墙体仍需校核；数量不是准确率。`pipeline-comparison.png` 可查看前后对比，`solid-walls.json` 是已扣除洞口的实墙几何。上一版及历史人工校正结果分别保存在 `output/legend-v1/`、`output/review-02/`。

## 测试与说明

```powershell
python -m unittest discover -s wall-recognition -p "test_*.py" -v
```

测试覆盖图像提取、逐段校正、直接修改、保存与撤销的接口流程。更多细节见 [程序说明](wall-recognition/README.md)。

仓库包含示例图及其识别、校正结果。用户在页面中保存的项目、旧反馈记录和运行缓存默认只留本地，不纳入版本管理。
