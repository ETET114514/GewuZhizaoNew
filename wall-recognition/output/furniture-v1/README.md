# 家具第一版核查

2026-09-10，使用当前 `evaluate_furniture.py` 和启动器对应的 Python 环境运行。

| 图 | 数据角色 | 三件主要家具命中 | 未匹配候选 |
| --- | --- | --- | --- |
| example-06 | 模板来源，校准 | 3/3（两床、一组沙发） | 0 |
| example-08 | 同户型彩色重绘 | 3/3（两床、一组沙发） | 0 |
| reference-04 | 未收入模板的另一种画法 | 0/3 | 0 |

每张图运行约 38–40 秒（本机，同期还运行了其他检查），不代表固定耗时。匹配要求类别相同且框 IoU 至少 0.5。结果、漏检框、候选来源与耗时在 `evaluation.json`；每个子目录包含家具框叠加图和对应 JSON。没有把同一户型的两个版本算作两个独立户型，也没有将模板自身匹配率称为通用准确率。

`page-desktop.png`、`page-mobile.png` 是新版页面操作截图。39 项完整回归通过；在最终最小图形过滤调整后，启动器环境下的四项家具测试再次通过，最终页面的家具编辑、框选补入、排除、撤销、保存及手机布局实测通过。未确认候选仍需人工校核。
