# 墙体识别 · 第 01 小步

本次交付：从一张户型图片自动提取**墙体候选**，生成编号叠加图和逐段坐标。全部处理在本机完成。

## 本地页面：识别 → 直接修改 → 保存项目

双击 `start-review.cmd` 启动，或运行 `python web_server.py --open-browser`。本地地址为 `http://127.0.0.1:8765/`。

1. 导入图片或使用示例图，点击「运行墙体识别」。此操作真实调用已有 Python 检测器。
2. 点选墙段，选择太短、太长、缺转角、位置、厚度或误识别等情况。
3. 指定方向、连接目标或像素调整量，点击「应用修改」。页面程序立即修改几何并更新图形，不需要助手转交处理。
4. 漏掉的墙可框选后应用；程序按矩形长边生成中心线、短边生成厚度，之后仍可调整。
5. 「撤销上一步」最多恢复最近 50 次修改；「保存项目」将当前底图、修改后的 `walls.json` 和 `changes.json` 保存到 `projects/日期时间-编号/`。

例子：选择 W005，选「墙段太短」、向右、连接到 W004；点击应用会将右端直接延长至 W004。选择 W008，填写「向下一直到 W013」，程序识别方向与目标后补竖向转角。转角默认从末端（右/下）开始，可以改选起端（左/上）。

这是本地确定性几何修改器：支持明确的类型、方向、像素量、垂直墙交点，以及少量中文关键词。不是任意自然语言模型。缺参数、方向错误、无法相交、越出图像时会明确报错并保留原图。长度单位是像素；当前延长轮廓不会自动判断新增部分是否属于门窗洞口，这部分留待后续实现。

本轮未实现项目重新导入；已保存的项目文件可供后续功能继续使用。重新识别会开始新一轮，需先保存已有修改。内存结果重启后清空，磁盘项目保留。上传支持 PNG/JPG/WebP，最多 12 MB、400 万像素；服务只监听本机回环地址。

验证覆盖：识别与已有程序一致；修改接口实际更新坐标；保存后的坐标与页面结果配套；撤销恢复原几何；中文方向/目标解析；补墙、删除及错误操作不污染原数据。前端完成语法检查，本轮未做浏览器点击或视觉 QA。可选 WebMCP 工具依浏览器能力启用，尚无已连接上下文验证；手动页面流程不依赖它。

原来的反馈接口保留为兼容代码，当前页面主流程执行的是直接修改及项目保存。

## 最新：第 02 次反馈校正

最新查看和编辑起点为 `output/review-02/walls.json`，带图例的结果在 `output/review-02/review.png`。

本轮按反馈完成五处连接：

- W010 右端向上补 W028，到 y=240 的门边。
- W002 右端向下补 W029，到 y=205 的门边；两竖段之间的厨房入口保留。
- W005 向右延至 W004 的 x=365 中心线。
- W008 右端向下补 W030，到 W013 的 y=372 中心线。
- W023 向右延至 W021 的 x=267.5 中心线。

上一轮的 W021 延长、W013 补齐、W027 短墙及 B001–B003 边界全部保留。增加的转角竖段单独编号，便于以后独立调整。

W005、W023、W029、W030 的完整中心线中包含原图可见的门窗位置，存为 `opening_hints`（待校核）。叠加图将这些区间画成绿色虚线；后续生成实体墙时应扣除这些区间，不能把整段中心线全部填成实墙。门窗类别和尺寸仍是按底图估计，本次没有训练或改变通用识别算法。

复现本轮校正（需先生成下文第 01 次结果）：

```powershell
python apply_corrections.py input/floorplan.png output/review-01/walls.json input/floorplan-corrections-02.json --output output/review-02
```

测试覆盖了五处连接、厨房入口保留、上一轮改动保留、门窗区间不填实、错误版本和越出所属墙段的门窗区间拒绝应用。

## 历史：第 01 次反馈校正

根据反馈“W021不够长 W014 W022外围缺了”，已保存一份独立校正结果：

- `output/review-01/review.png`：带图例的校正图。橙色为改动，绿色虚线为待分类外围边界，蓝色为其余原候选。
- `output/review-01/walls.json`：保留原编号，W021 上端从 y=515 延至 y=465，长度由 144 变为 194 像素；W013 向左补齐 W014 附近的上横墙，新增 W027 卫生间右上短墙。
- B001 补卫生间右侧，B002 / B003 补右下阳台右侧和底边。原图这些部位带开启符号，因此保存在 `boundary_candidates`，尚未确定门窗类型，不作为实墙封闭。

此版本为按反馈及底图估计的校正，全部仍待校核，**不是通用检测算法已自动修好这些情况**。原始 `output/walls.json` 与原识别图保留用于比较。校正文件绑定原图片哈希与被修改墙段的原坐标，避免应用到另一张图片或编号已改变的结果。

复现校正（在本目录运行）：

```powershell
python apply_corrections.py input/floorplan.png output/walls.json input/floorplan-corrections-01.json --output output/review-01
```

`output/review-01/walls.json` 保留为历史版本；后续使用上方最新版本。不要重新跑自动识别来覆盖人工校正。校正测试验证了长度与外框同步、原编号和未改段保持不变、门窗边界与实墙分离，以及错误图片、旧编号和越界数据拒绝应用。

## 先看结果

- `output/walls-overlay.png`：蓝色区域是自动提取的候选，编号对应坐标文件。
- `output/walls.json`：每一段有独立编号、起点、终点、厚度和待校核状态。
- `input/floorplan.png`：本次使用的原始户型图，745 × 761 像素。

本图输出 **26 段候选**。这不是 26 段已确认墙体，也不是准确率。

## 已知效果与限制

目视检查可见：左侧长外墙、上方横墙、中部卫生间隔墙、下方卧室的部分竖墙已被提取；床框误识别比初次参数减少。

仍存在漏识别和误识别：中间部分横墙、阳台浅色边界不完整；柜体和洁具周围的边框可能被当作墙。候选在厚度变化、交接点和门洞边缘的位置也可能不准，需要逐段校核。

当前算法按灰度、颜色差和连续线条筛选，支持近似水平、垂直的深灰墙线。它没有学习建筑语义，也没有使用人工标注数据计算准确率；不适用于任意风格、斜墙或透视照片。图像分辨率变化后需要调整像素参数。

所有尺寸单位均为**像素**。图中没有已知尺寸，因此 `scale_mm_per_px` 为 `null`。本结果不能用于施工尺寸。

## 运行

需要 Python 3.10 及以上。首次安装依赖：

```powershell
python -m pip install -r requirements.txt
```

在本目录运行：

```powershell
python recognize_walls.py input/floorplan.png --output output
```

识别另一张图片：

```powershell
python recognize_walls.py "你的户型图.png" --output another-result
```

同一输出目录中的 `walls.json` 和 `walls-overlay.png` 会被覆盖；要保留旧结果，请换一个输出目录。

调整参数示例：

```powershell
python recognize_walls.py input/floorplan.png --output experiment --threshold 170 --min-thickness 6
```

亮度阈值越高，浅灰区域越容易进入候选；最小厚度越大，细边框越容易排除，也越容易漏掉薄墙。`--help` 可查看全部参数。

## 坐标约定

原点在图片左上角，x 向右，y 向下。`start_px` / `end_px` 是候选中心线的两端，`thickness_px` 是候选宽度。这三者是编辑器的主要几何数据；`bbox_px`、`length_px`、`orientation` 是派生值，程序在修改几何后重新计算，并更新页面图形。

自动候选的 `review_status` 为 `unreviewed`；页面直接修改后的墙段标记为 `edited`。文件同时记录输入图片哈希及算法参数，便于后续重复比较。

## 验证

```powershell
python -m unittest discover -s . -p "test_*.py" -v
```

测试覆盖：水平/垂直墙几何、20 像素门洞保留、细家具线与彩色区域排除、空白图、图片边缘。这些是算法基础行为测试，不能代替真实户型图的准确率评估。

## 下次从这里继续

本地页面已支持点选、直接修改、撤销和保存。后续可把项目重新导入、端点拖动、比例尺校准分别拆成独立小步，再处理门窗识别与三维建模。每次限定一个可验证的小交付，控制在 30 分钟以内。
